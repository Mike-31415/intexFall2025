<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Event</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        /* Temporary styling – you can move this into styles.css later */
        body {
            font-family: Arial, sans-serif;
            margin: 30px;
        }

        h1 {
            margin-bottom: 20px;
        }

        .event-form {
            max-width: 600px;
        }

        .form-row {
            margin-bottom: 15px;
            display: flex;
            flex-direction: column;
        }

        label {
            font-weight: bold;
            margin-bottom: 5px;
        }

        input[type="text"],
        input[type="number"],
        textarea {
            padding: 8px;
            border-radius: 4px;
            border: 1px solid #ccc;
            font-size: 14px;
        }

        textarea {
            min-height: 80px;
            resize: vertical;
        }

        .buttons {
            margin-top: 20px;
            display: flex;
            gap: 10px;
        }

        .btn {
            padding: 8px 16px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 14px;
        }

        .btn-save {
            background-color: #4CAF50;
            color: white;
        }

        .btn-cancel {
            background-color: #9e9e9e;
            color: white;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }

        .error-message {
            color: #c62828;
            margin-bottom: 15px;
        }
    </style>
</head>

<body>
    <h1>Edit Event</h1>

    <% if (error_message) { %>
        <div class="error-message"><%= error_message %></div>
    <% } %>

    <!-- 
        Expecting an "event" object passed in from index.js, e.g.:
        res.render("editEvents", { event: someEventRow });
    -->
    <form class="event-form" action="/events/edit" method="POST">
        <!-- If you later use an ID from the database -->
        <input type="hidden" name="eventid" value="<%= event.eventid %>">

        <div class="form-row">
            <label for="eventname">Event Name</label>
            <!-- Often event name is the key, so you might keep it read-only -->
            <input type="text" id="eventname" name="eventname" 
                   value="<%= event.eventname %>" readonly>
        </div>

        <div class="form-row">
            <label for="eventtype">Event Type</label>
            <input type="text" id="eventtype" name="eventtype" 
                   value="<%= event.eventtype %>">
        </div>

        <div class="form-row">
            <label for="eventdescription">Description</label>
            <textarea id="eventdescription" name="eventdescription"><%= event.eventdescription %></textarea>
        </div>

        <div class="form-row">
            <label for="eventrecurrencepattern">Recurrence Pattern</label>
            <input type="text" id="eventrecurrencepattern" name="eventrecurrencepattern" 
                   value="<%= event.eventrecurrencepattern %>">
        </div>

        <div class="form-row">
            <label for="eventdefaultcapacity">Default Capacity</label>
            <input type="number" id="eventdefaultcapacity" name="eventdefaultcapacity" 
                   value="<%= event.eventdefaultcapacity %>">
        </div>

        <div class="buttons">
            <button type="submit" class="btn btn-save">Save Changes</button>
            <a href="/events" class="btn btn-cancel">Cancel</a>
        </div>
    </form>
</body>
</html>
