<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ella Rises – Home</title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <div class="page-wrapper">

        <h1 style="text-align:center; margin-top:40px;">Ella Rises Admin Home</h1>

        <p style="text-align:center; margin-top:10px;">
            <% 
                // UPDATE: Change role strings to 'Admin' and 'Participant'
                let roleLabel = (participant_role === 'admin' || participant_role === 'Admin' || participant_role === 'A') 
                    ? 'Admin' 
                    : 'Participant';
            %>
            Logged in as <strong><%= roleLabel %></strong>
            <% if (username) { %>
                ( <%= username %> )
            <% } %>
        </p>

        <div style="
            display:flex;
            flex-wrap:wrap;
            gap:15px;
            justify-content:center;
            margin-top:35px;
        ">
            <a href="/donations" class="btn btn-primary">Donations</a>
            <a href="/milestones" class="btn btn-primary">Milestones</a>
            <a href="/participants" class="btn btn-primary">Participants</a>
            <a href="/events" class="btn btn-primary">Events</a>
            <a href="/postsurveys" class="btn btn-primary">Post Surveys</a>

            <% 
                // UPDATE: Check participant_role for Admin status
                if (participant_role === 'admin' || participant_role === 'Admin' || participant_role === 'A') { 
            %>
                <a href="/users" class="btn btn-primary">Users</a>
            <% } %>
        </div>

    </div>
</body>
</html>