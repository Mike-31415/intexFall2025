<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Users List</title>
        <link rel="stylesheet" href="/styles.css">
    </head>

    <body>
        <%- include('partials/header') %>
        <div class="container mt-5">

            <h1 class="mb-4">Users List</h1>

            <!-- ADMINS SEE ADD BUTTON -->
            <% if (role === "admin") { %>
                <form action="/addParticipants" method="GET" class="mb-3">
                    <button class="btn btn-success" type="submit">Add User</button>
                </form>
            <% } %>

            <!-- ERROR MESSAGE -->
            <% if (error_message) { %>
                <div class="alert alert-danger">
                    <%= error_message %>
                </div>
            <% } %>

            <!-- NO USERS -->
            <% if (users.length === 0) { %>
                <div class="alert alert-warning">
                    No users found in the database.
                </div>
            <% } else { %>

                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Email</th>
                            <th>First Name</th>
                            <th>Last Name</th>
                            <th>Role</th>
                            
                            <!-- Admin-only columns -->
                            <% if (role === "admin") { %>
                                <th>Edit</th>
                                <th>Delete</th>
                            <% } %>

                        </tr>
                    </thead>

                    <tbody>
                        <% users.forEach(user => { %>
                            <tr>
                                <td><%= user.participant_email %></td>
                                <td><%= user.participant_first_name %></td>
                                <td><%= user.participant_last_name %></td>
                                <td><%= user.participant_role %></td>

                                <!-- EDIT: Admins only -->
                                <% if (role === "admin") { %>
                                    <td>
                                        <form action="/editParticipants/<%= user.participant_id %>" method="GET">
                                            <button class="btn btn-primary btn-sm">Edit</button>
                                        </form>
                                    </td>

                                    <!-- DELETE: Admins only -->
                                    <td>
                                        <form action="/deleteParticipants/<%= user.participant_id %>" method="POST">
                                            <button class="btn btn-danger btn-sm">Delete</button>
                                        </form>
                                    </td>
                                <% } %>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } %>

            <a href="/" class="btn btn-primary mt-3">Back to Dashboard</a>
            <a href="/logout" class="btn btn-secondary mt-3">Logout</a>
        </div>
    </body>
</html>
